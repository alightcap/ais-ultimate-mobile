import { StyleSheet } from "react-native";

export const Colors = {};

export const GlobalStyles = StyleSheet.create({
  container: {
    flex: 1,
    marginBottom: 16,
  },
  headingText: {
    fontSize: 20,
    textAlign: "center",
    margin: 5,
  },
});
